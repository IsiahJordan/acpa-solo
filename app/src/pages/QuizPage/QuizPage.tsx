import { Stack, Box, Typography } from '@mui/material'
import useNav from '@/hooks/useNav'

function QuizPage() {
  const { setQuery, getQuery} = useNav();
   

  return (
    <Stack maxHeight="100vh">
         
    </Stack>
  );
}

export default QuizPage;
